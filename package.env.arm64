# arm64 only. Installed as /etc/portage/package.env/cortex-a53-errata by
# gentoo-build/emerge.sh, the catalyst confdir and image-builder, keyed on
# the build arch. env/ is copied everywhere and inert without this
*/* cortex-a53-errata
